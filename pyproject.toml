[project]
name = "databricks-ai-bridge"
version = "0.21.1"
description = "Official Python library for Databricks AI support"
authors = [
    { name="Databricks", email="agent-feedback@databricks.com" },
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "typing_extensions>=4.15.0",
  "pydantic>=2.10.0",
  "databricks-sdk>=0.49.0",
  "databricks-ai-search>=0.73",
  "pandas>=2.2.0",
  "tiktoken>=0.8.0",
  "tabulate>=0.9.0",
  "mlflow-skinny>=3.10.1",
]

[project.license]
file = "LICENSE.txt"

[project.optional-dependencies]
memory = [
  "psycopg[binary,pool]>=3.2.10",
  "sqlalchemy[asyncio]>=2.0.0",
]
agent-server = [
  "databricks-ai-bridge[memory]",
  "fastapi>=0.129.0",
  "pydantic-settings>=2.0",
  "uvicorn>=0.20.0",
]

[dependency-groups]
dev = [
  "hatch==1.16.2",
  "ty==0.0.23",
  { include-group = "tests" },
  { include-group = "lint" },
]
tests = [
  "mlflow==3.10.1",
  "pytest==9.0.2",
  "pytest-asyncio==1.3.0",
  "pytest-cov==4.1.0",
]
doc = [
  "docutils==0.21.2",
  "sphinx==8.1.3",
  "sphinx-autobuild==2024.10.3",
  "sphinx_rtd_theme==3.0.2",
  "jinja2==3.1.6",
  "MarkupSafe==3.0.3",
  "commonmark==0.9.1",
  "myst-parser==4.0.1",
  "autodoc_pydantic==2.2.0",
  # Integration packages needed by autodoc
  "databricks-langchain",
  "databricks-openai",
  "databricks-dspy",
  "databricks-mcp",
]
lint = [
  "ruff==0.14.10",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
include = [
  "src/*"
]

[tool.hatch.build.targets.wheel]
packages = ["src/databricks_ai_bridge"]

[tool.ruff]
line-length = 100
target-version = "py39"
include = ["pyproject.toml", "src/**/*.py", "tests/**/*.py"]

[tool.ruff.lint]
select = [
  # isort
  "I",
  # bugbear rules
  "B",
  # remove unused imports
  "F401",
  # bare except statements
  "E722",
  # print statements
  "T201",
  "T203",
  # misuse of typing.TYPE_CHECKING
  "TC004",
  # import rules
  "TID251",
  # undefined-local-with-import-star
  "F403",
]

[tool.ruff.format]
docstring-code-format = true
docstring-code-line-length = 88

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.pytest.ini_options]
pythonpath = ["src"]
filterwarnings = [
    "ignore::Warning",
    "default::Warning:databricks_ai_bridge",
    "default::Warning:tests",
]

[tool.coverage.run]
source = ["databricks_ai_bridge"]
branch = true

[tool.coverage.report]
show_missing = true

[tool.ty.environment]
root = ["./src", "./tests"]

[tool.ty.src]
include = ["./src", "./tests"]
exclude = [
  # Fixture code and deploy scripts — not SDK code, uses MLflow/agents types that don't resolve
  "./tests/integration_tests/obo/model_serving_fixture",
  "./tests/integration_tests/obo/app_fixture",
  "./tests/integration_tests/obo/deploy_serving_agent.py",
]
